<% action_items.each do |action_item| %>
  <div class="note-in-column panel panel-primary">
    <div class="panel-heading"><%= link_to action_item.title, action_item_path(action_item) %></div>
    <div class="panel-footer">Assigned To: <%= action_item.assigned_to %></div>
  </div>
<%end %>
