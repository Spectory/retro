<%= will_paginate %>

<div class="container main-page-with-margin">
  <table class="table table-responsive table-hover">
    <thead>
      <tr >
        <th>Title</th>
        <th>Body</th>
        <th>Assigned To</th>
        <th>Note</th>
        <th>-</th>
        <th>-</th>
     </tr>
    </thead>
    <tbody>
      <%= render @action_items %>
    </tbody>
  </table>
  <%= button_to "New", new_note_path, method: :get, class: 'btn btn-primary'%>
</div>

<%= will_paginate %>
